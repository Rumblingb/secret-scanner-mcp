startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      proKey:
        type: string
        description: "Pro key for unlimited usage"
        default: ""
  commandFunction: |
    (config) => ({command: 'python3', args: ['server.py', '--pro-key', config.proKey || '']})
